%session.col-md-12.user-fundo.flashes
  #flash
    - flash.each do |type, message|
      .alert.alert-dismissable{ :class => twitterized_type(type) }
        %button.close{"data-dismiss" => "alert", "aria-hidden" => "true"} ×
        %p= message