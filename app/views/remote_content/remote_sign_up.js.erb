if($('.modal').length > 0) {
  $("#sign_up_modal").modal();
}
else {
  $(".flashes").append("<%= escape_javascript(render "remote_sign_up", :formats => [:html]) %>");
  $("#sign_up_modal").modal();
}